defmodule MapleWeb.Live.Components.Navbar do
  use MapleWeb, :live_component

  @impl true
  def mount(socket) do
    {:ok, socket}
  end

  @impl true
  def render(assigns) do
    ~L"""
      <div>AISJDIASJD</div>
    """
  end


end
